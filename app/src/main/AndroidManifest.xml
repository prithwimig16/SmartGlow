<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bhargav.myapplication">

    <application
        android:name=".SmartGlowApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".Splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home.HomeActivity" />
        <activity
            android:name=".deviceActivity.DeviceActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <service
            android:name="com.jingxun.meshlibtelink.TelinkLightService"
            android:exported="true" />

        <activity android:name=".BlueTooth.BlueToothActivity" />
        <activity android:name=".SearchDevices.SearchDevicesActivity" />
        <activity android:name=".DeviceFound.DeviceFoundActivity" />
        <activity android:name=".WelcomeHome.WelcomeHomeActivity"></activity>
    </application>

</manifest>